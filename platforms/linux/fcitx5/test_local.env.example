# Fcitx 5 测试模块本地配置模板。
# 复制为 test_local.env 后按本机情况填写；test_local.env 不进入 Git。
# 留空则使用默认值。

# 安装前缀，默认 /usr。
# PREFIX=/usr

# manifest.toml 路径覆盖；留空则使用 FFI 产物内的加密嵌入资源。
# GANNYU_MANIFEST=

# 默认区域覆盖；留空则用 manifest 的 default_region。
# GANNYU_REGION_ID=
